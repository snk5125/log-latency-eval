# =============================================================================
# roles/cribl-worker/handlers/main.yml
# =============================================================================
---
- name: Restart cribl worker
  ansible.builtin.systemd:
    name: "{{ cribl_service_name }}"
    state: restarted
    enabled: true
  # Restart reloads the worker-group local/ config.
